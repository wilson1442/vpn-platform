# -------------------------------------------------------
# VPN Node Agent — Environment Configuration
# -------------------------------------------------------
# Copy this file to .env and fill in the values.
# The AGENT_TOKEN must match the agentToken generated when
# registering this node via the admin panel or API.
# -------------------------------------------------------

# Token for authenticating with the API and receiving
# requests from OpenVPN scripts. Must match the agentToken
# from POST /vpn-nodes registration.
AGENT_TOKEN=

# Base URL of the VPN platform API
AGENT_API_BASE_URL=http://localhost:3000

# Port the agent HTTP server listens on
AGENT_PORT=3001

# OpenVPN management interface address
MGMT_HOST=127.0.0.1
MGMT_PORT=7505

# Path where the agent writes the CRL file for OpenVPN
CRL_PATH=/etc/openvpn/crl.pem
